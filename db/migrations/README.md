This directory is intentionally empty right now.

All previously accumulated schema changes have been squashed into `db/bootstrap/`.

Add new files here only for future post-bootstrap changes that should remain incremental.
