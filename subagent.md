# 하위 에이전트 작업 원칙

모든 하위 에이전트는 작업 전에 이 문서와 작업 대상의 `AGENTS.md`를 끝까지 읽는다. 메인 에이전트는 작업 지시마다 이 문서의 절대 경로, 대상 저장소, 담당 이슈와 수정 허용 파일을 명시한다.

## 1. No fallback / No mock

- 오류를 감추는 fallback, 임의 기본값, 가짜 성공 응답, mock 데이터·서비스를 추가하지 않는다. 검증도 mock 응답만으로 통과시키지 않는다.
- 실패하면 다른 API·필드·사전·저장 경로로 우회하는 처리를 만들지 않는다.
- 예외를 삼키거나, 저장 실패를 성공으로 표시하거나, 문제가 있는 데이터를 조용히 버리지 않는다.
- 검증·권한·잠금을 끄거나 느슨하게 만들어 오류를 없애지 않는다.
- 실제 UI → 요청 → 서버 처리 → DB → 재조회 경로에서 원인을 확인하고 수정한다. 준비된 테스트 데이터는 실제 저장·조회 경로로 검증한다.

## 2. 기존 코드와 구현 방향 유지

- 수정 전에 해당 코드의 호출부, 같은 기능의 다른 섹션, 기존 테스트를 찾아 비교한다.
- 기존 파일·함수·타입·필드·공통 컴포넌트·오류 처리 방식을 우선 사용한다.
- 기존 구조를 무시한 새 파일, 새 헬퍼, 새 함수, 새 필드, 별도 저장 경로를 임의로 만들지 않는다.
- 정말 필요하다면 기존 구조로 해결할 수 없는 근거와 최소 변경안을 먼저 메인 에이전트에게 제출한다. 승인 전에는 추가하지 않는다. 새 테스트 파일도 같은 원칙을 따른다.
- API 계약, DB 모델, 감사 이력, validation, 권한 정책을 편의상 바꾸지 않는다. 요구사항·정책이 모호하면 추측으로 구현하지 말고 보고한다.
- 같은 원인이 여러 호출부에 영향을 주면 기존 공통 경로에서 해결한다. 섹션마다 다른 예외 처리를 복제하지 않는다.

## 3. 최소 수정 원칙

- 배정된 오류의 근본 원인을 해결하는 데 필요한 변경만 한다.
- 관계없는 리팩터링, 이름 변경, 포맷 정리, 의존성 추가, 미래 기능용 구조를 끼워 넣지 않는다.
- 변경 줄 수를 줄이려고 필요한 검증·보안·데이터 보존·회귀 테스트를 생략하지 않는다.
- UI 문구만 바꾸거나 오류를 숨긴 것으로 저장·삭제 실패를 해결했다고 보고하지 않는다.
- 기존 미커밋 변경은 보존한다. 다른 에이전트의 변경을 덮어쓰거나 되돌리지 않는다.

## 병렬 작업과 승인

1. **모델:** 수정 담당 하위 에이전트는 Sol (`gpt-5.6-sol`)을 사용한다. 사용할 수 없으면 보고하고, 다른 모델로 조용히 대체하지 않는다.
2. **분담:** 메인 에이전트가 이슈·파일 소유권을 정한다. 서로 겹치지 않는 작업만 병렬로 진행한다. 공유 파일이나 공통 원인이 발견되면 메인에게 알리고 담당을 재조정한다.
3. **원인 확인:** 재현 조건, 원인 코드, 관련 호출부, 최소 수정 방향을 근거와 함께 보고한다. 범위 확대나 새 구성요소가 필요하면 승인부터 받는다.
4. **수정·검증:** 허용된 파일만 수정한다. 기존 테스트 위치에 회귀 검증을 보강하고 실제 저장·재조회 동작을 확인한다. 테스트하지 못한 부분을 통과로 표시하지 않는다.
5. **검토 요청:** 수정 결과는 승인 대기 상태로 메인에게 전달한다. 하위 에이전트가 스스로 최종 승인하지 않는다.
6. **메인 검토:** 메인이 diff와 관련 호출부를 직접 읽고, 원인 해결 여부·코드 일관성·최소 변경·테스트 결과를 확인한다. 필요하면 직접 재검증하고 보완을 요청한다.
7. **승인:** 메인이 이슈별로 승인/보완 필요와 근거를 기록한다. 승인된 변경만 최종 반영 대상으로 채택한다. 공유 작업 공간에 파일이 존재하는 것 자체는 승인이 아니다.
8. **커밋·푸시·배포:** 하위 에이전트가 임의로 실행하지 않는다. 메인도 사용자가 허용한 범위에서만 수행한다.

## 검증 환경과 데이터 보호

- 브라우저 UI 검증은 **인앱 브라우저만** 사용한다. Chrome은 사용하지 않는다. 다중 사용자 검증도 이 제한을 지키며, 불가능한 조건은 검증 완료로 처리하지 않고 보고한다.
- 기존 업무 Case·사용자·권한·조직 설정을 수정해 테스트를 통과시키지 않는다. 전용 테스트 데이터만 사용한다.
- 로컬 UI 검증은 `scripts/ui-env.sh <name> <backend-port> <frontend-port>`를 사용한다. 작업별로 분리된 DB를 쓰며 `app_db`를 재사용하거나 초기화하지 않는다.
- 저장소의 `target/`을 재사용한다. `CARGO_TARGET_DIR` 설정, `cargo run`, 동시 Cargo 실행은 금지한다. 메인이 빌드·테스트 실행을 조율한다.
- 백엔드 소스 변경 후 UI 검증 준비는 `cargo build -p web-server --bin web-server`를 한 번 실행한 뒤 해당 runner를 재시작한다. 프론트엔드 변경은 기존 개발 서버의 hot reload를 사용한다.
- named UI DB 초기화도 명시적으로 필요한 경우에만 한다. staging DB 초기화나 권한 우회는 하지 않는다.
- 저장·삭제 수정은 성공 응답뿐 아니라 새로고침·목록·상세 재조회까지 확인한다. 권한과 잠금이 관련되면 허용/거절 조건을 함께 확인한다.

## 하위 에이전트 결과 보고 형식

- **담당 이슈 / 수정 파일**
- **재현 조건과 근본 원인:** 코드 위치와 근거, 미확정 사항 구분
- **수정 내용:** 무엇을 바꿨는지, 기존 구현을 어떻게 재사용했는지
- **검증:** 실행한 명령·UI 동작, 실제 결과, 미실행 항목
- **영향 범위:** 관련 호출부·다른 섹션·데이터·권한에 대한 확인
- **원칙 점검:** fallback/mock 없음, 승인 없는 새 구성요소 없음, 관계없는 변경 없음
- **상태:** 검토 요청 / 보완 필요 / 차단 사유. 최종 승인은 메인이 기록

## 첫 수정 범위

보고서의 **1번 — 저장·삭제·값 유실**부터 진행한다:

- E001: LR 값과 NF 충돌 후 저장 실패
- E009: FDA Device 빈 하위 행 저장 실패
- E013: AE 삭제 시 리소스 오류
- E014: AE MFDS 긴 텍스트의 서버 거절
- E007: RE 편집값의 저장 연결 누락
- E016: CI·NR 미저장 이동 시 경고 없이 값 유실
- E012: LR·DH·LB·DG 삭제 저장 후 기존 행 복귀

이 목록은 증상 기준이다. 공통 원인이 확인되면 메인이 작업을 합치거나 재분배한다.

## 전체 목표와 순서

사용자의 후속 승인에 따라 전체 15종을 다음 순서로 수정·검토한다. 하위 에이전트는 메인이 배정한 항목만 수정하며 다음 그룹을 임의로 함께 수정하지 않는다.

1. 저장·삭제·값 유실: E001, E009, E013, E014, E007, E016, E012.
2. 화면 중단·Export: E005(DG 하위 항목 조합 화면 중단), E010(XML fragment export 실패).
3. 권한·잠금·상태: E015(다른 사용자 잠금 후 편집 상태), E017(Workflow 전환 권한 불일치), E011(Dashboard/상세 상태 불일치).
4. 필드 안내·표시: E003(길이 표시 불일치), E006(저장 차단 오류의 필드 표시 누락), E008(LB 목록 결과값 누락).

목표 완료는 구현뿐 아니라 메인 검토 및 관련 회귀 검증까지 끝난 상태다. 미검증이나 보완 중인 항목은 완료로 집계하지 않는다.
