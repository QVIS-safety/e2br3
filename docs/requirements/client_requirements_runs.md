# Client Requirements Run Plan

Source tracker: [client_requirements_todo.md](client_requirements_todo.md)

This file groups the remaining open `[ ]` and partial `[-]` tracker entries into coherent implementation runs. Use it as execution planning, not as replacement ground truth. Before starting any run, verify the linked tracker requirements against backend, frontend, tests, DB schema, API docs, and generated OpenAPI.

## Run Status Rules

- `[ ]` not started
- `[-]` in progress or partially completed
- `[x]` verified complete and reflected back into `client_requirements_todo.md`

## Progress Snapshot

- Baseline tracker count at creation: 99 done, 28 partial, 111 open, 238 total checklist entries.
- Baseline remaining tracker entries: 139 open-or-partial checklist entries.
- Baseline run plan: 45 remaining implementation runs.

## P0 Runs

- [x] Run 01 - Product naming, final app title, and configurable display strings. Completed with backend `/api/app/branding`, backend/static console branding, Next.js public-env branding helper, visible title/sidebar/login/dashboard copy updates, OpenAPI title cleanup, env defaults, and targeted backend/frontend tests.
- [x] Run 02 - QVIS, Client, Organization, and Sender source-of-truth decision and backend/admin cleanup. Completed with routing sender options sourced from INFO sender masters plus case headers, admin sender scope assignment using backend sender identifiers instead of template UUIDs, sender presave identifier typing/schema support, and backend/frontend targeted tests.
- [x] Run 03 - Sender defaults, organization/client linkage, and sender ownership authorization policy. Completed with persisted org-level sender default normalization, INFO UI default filtering based on master data, company sponsor-admin sender-scope assignment blocking per `roles.csv`, and sender/case ownership regression tests.
- [x] Run 04 - Idle session system configuration, warning lead time behavior, and admin controls. Completed with backend validation for system-level idle/warning settings, authenticated user readback, frontend timer normalization, and background refresh that no longer resets idle activity.
- [x] Run 05 - Global `QC` / `QCed` terminology cleanup across backend responses, filters, frontend copy, and tests. Completed dashboard, submission, admin-role copy, lifecycle filter labels, and default backend workflow description cleanup while preserving internal compatibility status codes.
- [x] Run 06 - Global menu placement for `DATA`, user info, and logout. Completed with sidebar footer user identity fallback/role display, consistent `Log out` wording, and a regression test that keeps `DATA` top-level and system-admin-only.
- [x] Run 07 - Permission model finalization for QC/lock wording, admin bypass policy, and workflow role edge cases. Verified and closed with audited sponsor-admin workflow override policy, locked/QCed edit blocking, workflow role/user/read-only edge-case tests, and frontend QC/lock wording checks.
- [x] Run 08 - Role/routing/scope frontend UAT cleanup for the static Role and Routing Console. Completed with final QC-only role privilege copy, company sponsor-admin sender-scope blocking in the static scope editor, inline script syntax validation, utilitarian console styling, and frontend static HTML regression coverage.
- [x] Run 09 - INFO, IMPORT, EXPORT, and SUBMISSION product/sender/study visibility UAT and remaining scope gates. Completed with a cross-history sender/product-scope regression for import, export, and submission history; fixed submission history to read under transaction-bound RLS and serialize camelCase fields expected by the frontend; restored role metadata compatibility fields and built-in-role edit denial.
- [x] Run 10 - Manual/imported case save parity page-level UAT and removal of normal-save import/batch/header noise. Completed with frontend case-identification save coverage for imported-case-shaped data, backend-field-banner mapping for message-date validation, and backend manual/imported save contract verification.
- [x] Run 11 - Save reason/comment policy for compliance-sensitive case updates beyond delete/status transitions. Completed by requiring `reason_for_change` for central case identity/scope updates, preserving normal manual/imported public-field saves, and verifying audit-log reason capture.
- [x] Run 12 - Delete confirmation copy and final no-password-delete UAT. Completed with frontend reason-only delete confirmation, DELETE request-body support, and CaseHeader regression coverage proving no password re-entry.
- [x] Run 13 - Case list export history from the case area with file, status, error, time, and user. Completed by verifying the case Review area uses the case-scoped export-history endpoint and adding regression coverage for file/status/error/time/user rendering and error-text download.
- [x] Run 14 - Follow-up draft creation from an existing case, including `C.1.1`, `C.1.2`, empty `C.1.5`, and source prefill. Completed with `followupOf` route loading, source-case clone helper, persistence-ID stripping, C.1.1 reuse, C.1.2/message-date reset, empty C.1.5, and focused frontend regression coverage.
- [x] Run 15 - Case-to-export/submission deep link with source case preselected and prefilled. Completed with case header `caseId` deep links, submission queue preselection/filter setup, authority prefill from linked case appendices, and focused frontend regression coverage.
- [x] Run 16 - Appendix selector top-level placement, duplicate selector removal, and duplication-check profile cleanup. Completed with shared `AppendixSelector` usage in the case header and duplication-check intake, case-appendix copy replacing validation-profile wording, and regression coverage for header selection, intake multi-appendix creation, and no legacy `validation_profile` payload.
- [x] Run 17 - MFDS/FDA/ICH regional rendering verification and removal of invalid regional fields. Completed with CASE section regional rendering regression coverage and a Section G fix that hides KR MPID/WHO-Drug fields unless MFDS is selected.
- [x] Run 18 - Required date/null-flavor re-audit for `C.1.2`, `C.1.4`, `C.1.5`, `E.i.4`, AE start dates, and sampled full-form gaps. Completed with frontend UI-binding coverage for C.1 required-date `NI` toggles and E.i.4 `UNK` toggles, plus backend validation reruns for C/E/F/G sampled null-flavor behavior.
- [x] Run 19 - E2B business-rule re-audit across all CASE tabs, including duplication-check report-type matrix. Completed the concrete C.1.3 duplication-check matrix with backend duplicate-basis enforcement, frontend intake gating, null-flavor-as-missing handling, and regression coverage; broader CASE business-rule audit remains tracked by the umbrella CASE re-audit item.
- [x] Run 20 - Validation markers and user-facing warning terminology across section/subsection red dots and blocking/non-blocking copy. Completed with frontend marker derivation from backend section/subsection summaries, all section-tab red-dot suites, validation integration coverage, and user-facing header labels that avoid internal blocking/non-blocking terminology.
- [x] Run 21 - Date-picker frontend behavior: English locale, future-date blocking, partial/UK-style requirements where applicable. Completed with shared E2B date helper coverage for UK-style normalization, partial precision preservation, and future-date detection; C.1 calendar English/future-day coverage; and duplication-check intake blocking for future C.1.5/E.i.4 dates.
- [x] Run 22 - Repeatable CASE line-list UI gaps for remaining `r`, `i`, and `k` structures. Completed as an audit/tracker closure: all `list UI.csv` repeatables are already implemented with simple line-list tables or table + detail views, and focused UI binding/regional tests passed.
- [x] Run 23 - CASE field-level `...` actions: audit trail, notation, and clear/delete value. Completed with a field-actions dialog regression covering notation, audit trail loading, and generic clear-field-value behavior for the current CASE field.
- [x] Run 24 - CASE audit trail payload/UI: timestamp, user, field, value, null flavor, notation, reason, newest first. Completed with CASE Audit Trail tab columns for timestamp, user, field name, value, null flavor, notation, and reason, plus newest-first API ordering regression coverage.
- [x] Run 25 - Unsaved-change prompts before navigation away from case pages. Completed with beforeunload and same-window link navigation regression coverage plus a hardened link guard that queues the existing unsaved Save/Leave dialog reliably.
- [x] Run 26 - Country input standardization to ISO 3166-1 alpha-2 list behavior. Completed by converting Study Registration Country to the shared ISO 3166-1 alpha-2 country autocomplete, matching the existing reporter/sender/reaction/drug country behavior.
- [x] Run 27 - MedDRA UX: release/version selection, LLT search, code+term display, and cross-section consistency. Completed with shared CASE MedDRA autocomplete lookup enforcing LLT search, labels that show term plus code, metadata that carries term/level/version for version population, and targeted frontend regression coverage.
- [x] Run 28 - WHO-Drug and UCUM data coverage, including LB `F.r.3.3` and MFDS DG WHO-DD behavior. Completed with backend UCUM seed/compatibility coverage for common lab units, frontend UCUM option mapping and LB `F.r.3.3` autocomplete, and WHO-Drug code/term/version metadata regression coverage for MFDS DG autocompletes.
- [x] Run 29 - Duplication-check partial-signature behavior and Product ID loaded-value correctness. Completed by verifying existing backend partial-signature duplicate behavior and fixing product-template intake selection so Product ID uses loaded identifier fields before display labels.
- [x] Run 30 - Export/submission receiver model separation from INFO receiver master data. Completed by removing routing identifier controls from the INFO receiver master form, adding explicit Export / Submission routing receiver identifier inputs, and verifying routing precedence with fallback support for existing receiver-template data.
- [x] Run 31 - Remove or relocate Message Header / Receiver Information from SD if still present. Completed by removing Message Header and Receiver Information controls from SD/C.3 and verifying the page no longer renders receiver routing fields.
- [x] Run 32 - Submission history details: batch result, message result, acknowledged date, ICSR count, and data file link. Completed with backend history API fields/OpenAPI docs and dashboard columns/links for batch/message result data, acknowledged date, per-row ICSR count, and XML data-file download; verified by `test_submission_history_includes_latest_ack_time_and_event`, `submission-history-details`, `cargo fmt --all`, and `npx tsc --noEmit`.
- [x] Run 33 - Submission/export queue and history columns, including event column and requested queue metadata. Completed by expanding the Export / Submission queue table with Most Recent Info, WW Unique No, Type of Report, Sender, Event (MedDRA), Submission, and Deleted columns, while Run 32 covers the requested Submission History detail columns; verified by `submission-history-details`, `submission-deep-link`, `submission-receiver-routing`, and `npx tsc --noEmit`.
- [x] Run 34 - Export/submission advanced search-based case selection by page/section, field, operator, and value. Completed with an advanced queue search panel and shared matcher supporting page/section targeting, field targeting, contains/equals/prefix/suffix/not-contains/empty operators, and value matching; verified by `submission-filters`, `submission-history-details`, `submission-deep-link`, and `npx tsc --noEmit`.
- [x] Run 35 - Export/submission simple filters: sender, case number, and ack accept status if required. Completed by adding explicit Sender and ACK Accept Status queue filters alongside the existing Case Number/QC/lock/workflow filters; verified by `submission-history-details`, `submission-filters`, `submission-deep-link`, and `npx tsc --noEmit`.
- [x] Run 36 - Excel line-list export. Completed with an Excel-readable `.xls` line-list export for the currently filtered Export / Submission queue, including the requested queue metadata columns; verified by `submission-line-list-export`, `submission-history-details`, `submission-filters`, and `npx tsc --noEmit`.
- [x] Run 37 - Imported-case export behavior for incomplete or mismatched sender/receiver/header values. Completed by verifying backend raw imported XML export and dirty-section C/header patching behavior, plus adding a dashboard regression that keeps clean imported raw XML exportable and excludes dirty imported XML until QCed; verified by `test_single_export_rejects_unselected_appendix_profile`, `submission-history-details`, and `npx tsc --noEmit`.
- [x] Run 38 - Receiver routing enforcement for authority/report type, `N.1.4`, and `N.2.r.3`. Completed with strict pre-export/pre-submission receiver routing validation requiring both N.1.4 and N.2.r.3 after resolving explicit routing inputs, authority/report-type receiver-template rules, and existing header values; verified by `submission-receiver-routing`, `submission-history-details`, and `npx tsc --noEmit`.
- [x] Run 39 - Review/RE timeline receiver data loading. Completed by verifying the case-detail API maps `review_receivers_json` into `reviewReceivers` for the Review/RE receiver due/reported-date timeline table; verified by `api.endpoints` and `npx tsc --noEmit`.
- [x] Run 40 - INFO list and record UX: labels, required markers, notation placement, header filters, deleted visibility, row-click edit. Completed with INFO header-cell filters, visible Active/Deleted status, row-click/keyboard edit behavior, record naming/label polish, and required-marker/notation placement cleanup; verified by `info-page` and `npx tsc --noEmit`.
- [x] Run 41 - INFO field-level audit trail access aligned with CASE behavior. Completed with CASE-style INFO field action buttons for record fields, local notation controls, and recent presave-template audit history from `/api/presave-templates/{id}/audit`; verified by `presave-info-audit`, `info-page`, and `npx tsc --noEmit`.
- [x] Run 42 - Study/Product master data: product semantics, multi-select, MFDS/FDA study regional fields, and CASE mapping decision. Completed by replacing free-text INFO Study product entry with multi-select registered Product master selection, adding Study-side MFDS/FDA regional fields and C.5.3 study/protocol choice, and mapping supported Study fields into CASE including `C.5.4.KR.1`; verified by `study-product-master`, `ui-binding`, `regional-rendering`, and `npx tsc --noEmit`.
- [x] Run 43 - Narrative element-ID composition scope decision and implementation or explicit deferral. Completed by removing the deprecated `Additional Narrative Fields` INFO label, retaining structured H-field narrative template support, and explicitly deferring full cubeSAFETY-style element-ID NR Auto composition as future scope per the 2026-04-02 PV comment; verified by `narrative-scope` and `npx tsc --noEmit`.
- [x] Run 44 - Admin role/user UX: `Role Setting` vs `Role & Privilege`, workflow UX, user deletion, one-admin rule, large-list scope picker, comments-only decision. Completed with consolidated `Roles & Privileges` wording, comments-only user form/display, editable user comments, user-delete action coverage, single-admin-role UI guard, and searchable sender/product/study scope pickers; verified by `admin-users.header-filters` and `npx tsc --noEmit`.
- [x] Run 45 - Admin/user and P1 UAT closeout: organization screen removal, user edit/delete flow, dashboard appendix behavior, To Do workflow relation, case-list header filters, section-specific rechecks, import timestamp/error-detail duplicate tracker cleanup. Run 45 completed ADMIN organization-panel removal, user create/edit/delete UX coverage, dashboard appendix/workflow To Do coverage, CASE list header filters, CI/RP/SD/AE/LR/DG rechecks, and import timestamp/error-text tracker cleanup; LR and DG closure is covered by `field-bindings`.

## Final Tracker Extension

- [x] Run 46 - Tracker reconciliation and explicit deferrals for completed summary duplicates. Completed by reconciling duplicate follow-up, export/submission, admin UAT, appendix selector, save/delete policy, workflow role-routing, and notation auto-translation decision entries against completed Runs 07, 10-16, 23-24, 30-38, 40-45.
- [x] Run 47 - Final CASE validation audit: close or implement the remaining full-form null-flavor and E2B business-rule umbrella items across CASE tabs, avoiding duplicate work already covered by Runs 18-23 and Run 45. Completed by fixing C.1 null-flavor save-fields audit identifiers, verifying supported null-flavor save-field coverage, rerunning backend validation C/D/E/F/G/H/N rule coverage, and rerunning frontend validation/UI-binding suites.
