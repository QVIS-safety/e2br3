# Narrative Presave -> NR Reference Matrix

Scope: Narrative Presave -> case NR/H narrative section.

Reference routes/evidence:

- ICH: `/en/NA/case/786994/detail/NR`
- FDA/US: `/en/US/case/786994/detail/NR`
- MFDS/KR: `/en/KR/case/786994/detail/NR`
- Captured in `docs/superpowers/specs/2026-05-30-nr-reference-ui-gap-design.md`.

Local modules compared:

- Backend presave model: `crates/libs/lib-core/src/model/presave.rs`
- Backend presave REST: `crates/services/web-server/src/web/rest/section_presave_rest.rs`
- Backend presave schema: `db/bootstrap/01-safetydb-schema.sql`
- Backend case target model/schema: `crates/libs/lib-core/src/model/narrative.rs`, `db/bootstrap/08-narrative-information.sql`
- Frontend presave form/types/mappers: `components/presave/NarrativeForm.tsx`, `lib/types/presave.ts`, `lib/schemas/presave.ts`, `lib/presave/canonicalMappers.ts`, `lib/presave/canonicalWriteMappers.ts`, `lib/hooks/usePresaveTemplates.ts`
- Frontend case target: `components/case-form/sections/SectionH.tsx`

Categories:

- `referenceImportedToCase`: reference-visible NR/H case field and local narrative presave import should populate the case section.
- `referencePreserveOnly`: local presave authoring or compatibility field that may remain on the presave but must not populate the NR/H case section.
- `localSystemOnly`: local identity, metadata, row IDs, soft delete, sequence, linkage, audit, or persistence mechanics.
- `removed`: fake, redundant, legacy, or unsupported field that should not remain in presave API/form/schema paths.

| field | reference evidence | canonical frontend source | canonical backend source | allowed read aliases | allowed write keys | case import target | duplicate sources found | category | decision | tests required |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| narrative_presaves.id / id | No reference case field; local row identity only. | `NarrativePresaveData.id` used for edit/audit. | `NarrativePresave.id` primary key. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | Empty | None found. | `localSystemOnly` | Keep for identity and audit. | Identity, persistence, RLS/audit, or local display/helper coverage as applicable. |
| narrative_presaves.organization_id | No reference case field; tenant scope only. | Not user-editable. | `NarrativePresave.organization_id`. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | Empty | None found. | `localSystemOnly` | Keep. | Identity, persistence, RLS/audit, or local display/helper coverage as applicable. |
| narrative_presaves.name / name | Presave list identity, not NR/H case content. | Presave template name outside section import payload. | Required `NarrativePresaveForCreate.name`. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | Empty | None found. | `localSystemOnly` | Keep as template identity; do not import. | Identity, persistence, RLS/audit, or local display/helper coverage as applicable. |
| narrative_presaves.comments / comments | Presave management metadata, not NR/H case content. | Presave comments/description metadata. | `comments` on parent presave. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | Empty | None found. | `referencePreserveOnly` | Keep on presave; do not import. | Mapper/write mapper and backend API/model coverage; negative import coverage where applicable. |
| narrative_presaves.deleted / deleted | No reference case field; local soft-delete state. | `deleted` flag in presave data/form. | `deleted` parent column. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | Empty | None found. | `localSystemOnly` | Keep for soft delete; do not import. | Identity, persistence, RLS/audit, or local display/helper coverage as applicable. |
| narrative_presaves.case_narrative / caseNarrative | Reference NR shows H.1 Case Narrative for ICH/FDA/MFDS. | `NarrativeForm`, canonical mapper/write mapper, and `SectionH.handleImport`. | Parent presave field exists. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | `narrative.caseNarrative` / `narrative_information.case_narrative`. | None found. | `referenceImportedToCase` | Import into H.1 and prove with actual `SectionH` import test. | Mapper/write mapper, form/schema, backend API/model, and actual section import or negative import coverage. |
| narrative_presaves.case_narrative_notation / caseNarrativeNotation | No NR/H case target in reference evidence. | `NarrativeForm` renders Notation for Auto Narrative; mappers preserve it. | Parent presave field exists. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | Empty | None found. | `referencePreserveOnly` | Keep as presave authoring metadata; do not import. | Mapper/write mapper and backend API/model coverage; negative import coverage where applicable. |
| narrative_presaves.case_summary / caseSummary | Reference NR case target is repeatable H.5.r summary text, not parent-level case summary. | `NarrativePresaveData.caseSummary`, write mapper sends `case_summary`, `SectionH.handleImport` imports to first summary text. | No current parent BMC/schema field. | None. | None. | `caseSummaryInformation[0].summaryText` if preserved. | Parent/alias summary source removed; `caseSummaries[].summaryText` is the canonical summary text source. | `removed` | Remove parent-level presave API/form/schema path; use `case_summaries[].summary_text` for H.5.r. | Negative mapper/write/import/API tests prove the field is ignored or absent. |
| narrative_presaves.additional_information / additionalInformation | Reference NR shows Additional Information (NR_SPONSOR) for ICH/FDA/MFDS. | `NarrativePresaveData.additionalInformation`; `NarrativeForm`; `narrativePresaveSchema.additionalInformation`; `SectionH.handleImport`. | `narrative_presaves.additional_information`; `NarrativePresave.additional_information`; `NarrativePresaveForCreate/Update.additional_information`. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | `additional_information` | `narrative.additionalInformation` / `narrative_information.additional_information`. | None found. | `referenceImportedToCase` | Keep one parent source and import into NR_SPONSOR. | Mapper/write mapper, form/schema, backend API/model, and actual Section H import coverage. |
| narrative_presaves.reporter_comments / reporterComments | Reference NR shows H.2 Reporter's comments. | `NarrativeForm`, canonical mapper/write mapper, and `SectionH.handleImport`. | Parent presave field exists. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | `narrative.reporterComments` / `narrative_information.reporter_comments`. | None found. | `referenceImportedToCase` | Import into H.2 and prove with actual `SectionH` import test. | Mapper/write mapper, form/schema, backend API/model, and actual section import or negative import coverage. |
| narrative_presaves.sender_comments / senderComments | Reference NR shows H.4 Sender's comments. | `NarrativeForm`, canonical mapper/write mapper, and `SectionH.handleImport`. | Parent presave field exists. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | `narrative.senderComments` / `narrative_information.sender_comments`. | None found. | `referenceImportedToCase` | Import into H.4 and prove with actual `SectionH` import test. | Mapper/write mapper, form/schema, backend API/model, and actual section import or negative import coverage. |
| narrative_presave_sender_diagnoses.id / senderDiagnoses[].id | No reference case field; child row identity only. | Child row id used for edit/audit/delete. | Child primary key. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | Empty | None found. | `localSystemOnly` | Keep for identity and updates. | Identity, persistence, RLS/audit, or local display/helper coverage as applicable. |
| narrative_presave_sender_diagnoses.narrative_presave_id | No reference case field; child parent FK only. | Not user-editable. | Child FK. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | Empty | None found. | `localSystemOnly` | Keep. | Identity, persistence, RLS/audit, or local display/helper coverage as applicable. |
| narrative_presave_sender_diagnoses.sequence_number / senderDiagnoses[].sequenceNumber | Reference H.3.r is repeatable; local order mechanic. | Child row order. | Child `sequence_number`. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | `narrative.senderDiagnoses[].sequenceNumber` / `sender_diagnoses.sequence_number`. | None found. | `localSystemOnly` | Keep to preserve repeat order; import as row order. | Identity, persistence, RLS/audit, or local display/helper coverage as applicable. |
| narrative_presave_sender_diagnoses.diagnosis_meddra_version / senderDiagnoses[].diagnosisMeddraVersion | Reference NR shows H.3.r.1a MedDRA Version. | Child row field and mapper. | Child field exists. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | `narrative.senderDiagnoses[].diagnosisMeddraVersion` / `sender_diagnoses.diagnosis_meddra_version`. | None found. | `referenceImportedToCase` | Import all non-deleted diagnosis rows. | Mapper/write mapper, form/schema, backend API/model, and actual section import or negative import coverage. |
| narrative_presave_sender_diagnoses.diagnosis_meddra_code / senderDiagnoses[].diagnosisMeddraCode | Reference NR shows H.3.r.1b diagnosis MedDRA code. | Child row field and mapper. | Child field exists. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | `narrative.senderDiagnoses[].diagnosisMeddraCode` / `sender_diagnoses.diagnosis_meddra_code`. | None found. | `referenceImportedToCase` | Import all non-deleted diagnosis rows. | Mapper/write mapper, form/schema, backend API/model, and actual section import or negative import coverage. |
| narrative_presave_sender_diagnoses.deleted / senderDiagnoses[].deleted/_delete | No reference case field; local soft-delete state. | Child soft delete/cancel support. | Child `deleted` column. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | Empty | None found. | `localSystemOnly` | Keep for presave graph edits; do not import deleted rows. | Identity, persistence, RLS/audit, or local display/helper coverage as applicable. |
| narrative_presave_case_summaries.id / caseSummaries[].id | No reference case field; child row identity only. | Child row id in type/mappers. | Child primary key. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | Empty | None found. | `localSystemOnly` | Keep for identity and updates. | Identity, persistence, RLS/audit, or local display/helper coverage as applicable. |
| narrative_presave_case_summaries.narrative_presave_id | No reference case field; child parent FK only. | Not user-editable. | Child FK. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | Empty | None found. | `localSystemOnly` | Keep. | Identity, persistence, RLS/audit, or local display/helper coverage as applicable. |
| narrative_presave_case_summaries.sequence_number / caseSummaries[].sequenceNumber | Reference H.5.r is repeatable; local order mechanic. | Child row order in type/mappers. | Child `sequence_number`. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | `caseSummaryInformation[].sequenceNumber` / `case_summary_information.sequence_number`. | None found. | `localSystemOnly` | Keep to preserve repeat order; import as row order. | Identity, persistence, RLS/audit, or local display/helper coverage as applicable. |
| narrative_presave_case_summaries.summary_type / caseSummaries[].summaryType | Reference NR does not show Summary Type. Existing NR spec keeps backend summary type only for backward compatibility. | Type/mappers preserve it; should not be visible/import-driving. | Child field exists. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | Backend case compatibility field only. | None found. | `referencePreserveOnly` | Keep for compatibility; do not show or require for import. | Mapper/write mapper and backend API/model coverage; negative import coverage where applicable. |
| narrative_presave_case_summaries.language_code / caseSummaries[].languageCode | Reference NR shows H.5.r.1b Language. | Type/mappers preserve it. | Child field exists. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | `caseSummaryInformation[].languageCode` / `case_summary_information.language_code`. | None found. | `referenceImportedToCase` | Import all non-deleted summary rows with language/text content. | Mapper/write mapper, form/schema, backend API/model, and actual section import or negative import coverage. |
| narrative_presave_case_summaries.summary_text / caseSummaries[].summaryText | Reference NR shows H.5.r.1a Case summary and Reporter's Comments. | `NarrativePresaveData.caseSummaries[].summaryText`; `NarrativeForm`; `narrativePresaveSchema.caseSummaries[].summaryText`; `SectionH.handleImport`. | `narrative_presave_case_summaries.summary_text`; `NarrativePresaveCaseSummary.summary_text`. | `summary_text` read only | `case_summaries[].summary_text` | `caseSummaryInformation[].summaryText` / `case_summary_information.summary_text`. | Parent/alias `caseSummary` source removed; this is the single active summary text source. | `referenceImportedToCase` | Import all non-deleted summary rows with language/text content. | Mapper/write mapper, form/schema, backend API/model, and actual Section H import coverage. |
| narrative_presave_case_summaries.caseSummary / caseSummaries[].caseSummary | Compatibility alias for `summaryText`; no distinct reference field. | Absent from active Narrative Presave type/schema/read/write mappers. | No backend column. | None. | None. | Empty | Parent/alias summary source removed; `caseSummaries[].summaryText` is the canonical summary text source. | `removed` | Keep removed from saved payload/form expectations; use `summaryText`. | Negative mapper/write/import/API tests prove the field is ignored or absent. |
| narrative_presave_case_summaries.deleted / caseSummaries[].deleted/_delete | No reference case field; local soft-delete state. | Child soft delete support in types/mappers. | Child `deleted` column. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | Empty | None found. | `localSystemOnly` | Keep for presave graph edits; do not import deleted rows. | Identity, persistence, RLS/audit, or local display/helper coverage as applicable. |
| created_at / updated_at / created_by / updated_by | No reference case field; local audit mechanics. | Not editable. | Parent and child audit columns. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | Empty | None found. | `localSystemOnly` | Keep. | Identity, persistence, RLS/audit, or local display/helper coverage as applicable. |

Coverage check:

- total reference fields: 9 (`H.1`, `H.2`, `H.3.r.1a`, `H.3.r.1b`, `H.4`, `H.5.r.1a`, `H.5.r.1b`, `NR_SPONSOR`, repeatable row ordering)
- total local fields: 27 rows in this matrix
- total categorized rows: 27
- zero uncategorized fields
- zero ambiguous fields
