# Sender Presave -> C.3 Reference Matrix

## Scope

Sender Presave master data and import into the case Sender section, `C.3 - Information on Sender of Case Safety Report`, plus the FDA/MFDS gateway value used for `messageHeader.messageSenderIdentifier`.

Reference basis:

- Live reference UI inspection on 2026-06-01 at `edu-safetyr3.crscube.io/57/508/en/KR/caseinfo/sender/508/detail`.
- Existing local C.3 import behavior in `frontend/E2BR3-frontend/components/case-form/sections/SectionC3.tsx`.
- Existing backend canonical sender schema in `sender_presaves`, `sender_presave_gateways`, and `sender_presave_responsible_persons`.

The live reference also shows PMDA/NMPA/EMA gateway rows; those are intentionally out of local scope. Local sender gateway alignment is limited to FDA and MFDS.

## Matrix

| field | reference evidence | canonical frontend source | canonical backend source | allowed read aliases | allowed write keys | case import target | duplicate sources found | category | decision | tests required |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `senderType` | C.3.1 sender type | `SenderPresaveData.senderType`; `SenderForm`; `senderPresaveSchema.senderType` | `sender_presaves.sender_type`; `SenderPresave.sender_type`; `SenderPresaveForCreate/Update.sender_type` | `senderType`, `sender_type` read only | `sender_type` | `safetyReportIdentification.senderType` | None | `referenceImportedToCase` | Keep one parent source and import exactly. | Mapper/write mapper, form/schema, backend API/model, Section C.3 import |
| `senderOrganization` | C.3.2 sender organisation | `SenderPresaveData.senderOrganization`; `SenderForm`; `senderPresaveSchema.senderOrganization` | `sender_presaves.organization_name`; `SenderPresave.organization_name`; `SenderPresaveForCreate/Update.organization_name` | `senderOrganization`, `organizationName`, `organization_name` read only | `organization_name` | `safetyReportIdentification.senderOrganization` | None | `referenceImportedToCase` | Keep one parent source and import exactly. | Mapper/write mapper, form/schema, backend API/model, Section C.3 import |
| `senderOrganizationNotation` | live detail shows C.3.2 `Notation` under Sender's Organisation | `SenderPresaveData.senderOrganizationNotation`; `SenderForm`; `senderPresaveSchema.senderOrganizationNotation` | `sender_presaves.organization_name_notation`; `SenderPresave.organization_name_notation`; `SenderPresaveForCreate/Update.organization_name_notation` | `senderOrganizationNotation`, `organizationNameNotation`, `organization_name_notation` read only | `organization_name_notation` | Empty | None | `referencePreserveOnly` | Preserve in sender presave; no C.3 case target. | Mapper/write mapper, form/schema, backend API/model, no case import |
| `senderDepartment` | C.3.3.1 sender department | `SenderPresaveData.senderPersons[].department`; `SenderForm` row `senderPersons.*.department`; `senderPresaveSchema.senderPersons[].department` | `sender_presave_responsible_persons.department`; `SenderPresaveResponsiblePerson.department` | `senderPersons[].department`, `responsiblePersons[].department` read only | `responsible_persons[].department` | `safetyReportIdentification.senderDepartment` | Legacy parent `senderDepartment` / `sender_presaves.department` migrated and dropped | `referenceImportedToCase` | Keep child row source only; import default responsible row. | Mapper/write mapper rejects parent, schema strips parent, backend parent absence/API details, Section C.3 child-only import |
| `senderPersonTitle` | C.3.3.2 sender title | `SenderPresaveData.senderPersons[].title`; `SenderForm` row `senderPersons.*.title`; `senderPresaveSchema.senderPersons[].title` | `sender_presave_responsible_persons.person_title`; `SenderPresaveResponsiblePerson.person_title` | `title`, `personTitle`, `person_title` read only | `responsible_persons[].person_title` | `safetyReportIdentification.senderPersonTitle` | Legacy top-level `senderPersonTitle` rejected by write/import tests | `referenceImportedToCase` | Keep child row source only; import default responsible row. | Mapper/write mapper, form/schema, backend API/model, Section C.3 child-only import |
| `senderPersonGivenName` | C.3.3.3 sender given name | `SenderPresaveData.senderPersons[].givenName`; `SenderForm` row `senderPersons.*.givenName`; `senderPresaveSchema.senderPersons[].givenName` | `sender_presave_responsible_persons.person_given_name`; `SenderPresaveResponsiblePerson.person_given_name` | `givenName`, `personGivenName`, `person_given_name` read only | `responsible_persons[].person_given_name` | `safetyReportIdentification.senderPersonGivenName` | Legacy parent `senderPersonGivenName` / `sender_presaves.person_given_name` migrated and dropped | `referenceImportedToCase` | Keep child row source only; import default responsible row when present. | Mapper/write mapper rejects parent, schema validation, backend parent absence/API details, Section C.3 child-only import |
| `senderPersonMiddleName` | C.3.3.4 sender middle name | `SenderPresaveData.senderPersons[].middleName`; `SenderForm` row `senderPersons.*.middleName`; `senderPresaveSchema.senderPersons[].middleName` | `sender_presave_responsible_persons.person_middle_name`; `SenderPresaveResponsiblePerson.person_middle_name` | `middleName`, `personMiddleName`, `person_middle_name` read only | `responsible_persons[].person_middle_name` | `safetyReportIdentification.senderPersonMiddleName` | Legacy top-level `senderPersonMiddleName` rejected by write/import tests | `referenceImportedToCase` | Keep child row source only; import default responsible row. | Mapper/write mapper, form/schema, backend API/model, Section C.3 child-only import |
| `senderPersonFamilyName` | C.3.3.5 sender family name | `SenderPresaveData.senderPersons[].familyName`; `SenderForm` row `senderPersons.*.familyName`; `senderPresaveSchema.senderPersons[].familyName` | `sender_presave_responsible_persons.person_family_name`; `SenderPresaveResponsiblePerson.person_family_name` | `familyName`, `personFamilyName`, `person_family_name` read only | `responsible_persons[].person_family_name` | `safetyReportIdentification.senderPersonFamilyName` | Legacy top-level `senderPersonFamilyName` rejected by write/import tests | `referenceImportedToCase` | Keep child row source only; import default responsible row. | Mapper/write mapper, form/schema, backend API/model, Section C.3 child-only import |
| `senderStreetAddress` | C.3.4.1 street | `SenderPresaveData.senderStreetAddress`; `SenderForm`; `senderPresaveSchema.senderStreetAddress` | `sender_presaves.street_address`; `SenderPresave.street_address`; `SenderPresaveForCreate/Update.street_address` | `senderStreetAddress`, `streetAddress`, `street_address` read only | `street_address` | `safetyReportIdentification.senderStreetAddress` | None | `referenceImportedToCase` | Keep one parent source and import exactly. | Mapper/write mapper, form/schema, backend API/model, Section C.3 import |
| `senderCity` | C.3.4.2 city | `SenderPresaveData.senderCity`; `SenderForm`; `senderPresaveSchema.senderCity` | `sender_presaves.city`; `SenderPresave.city`; `SenderPresaveForCreate/Update.city` | `senderCity`, `city` read only | `city` | `safetyReportIdentification.senderCity` | None | `referenceImportedToCase` | Keep one parent source and import exactly. | Mapper/write mapper, form/schema, backend API/model, Section C.3 import |
| `senderState` | C.3.4.3 state | `SenderPresaveData.senderState`; `SenderForm`; `senderPresaveSchema.senderState` | `sender_presaves.state`; `SenderPresave.state`; `SenderPresaveForCreate/Update.state` | `senderState`, `state` read only | `state` | `safetyReportIdentification.senderState` | None | `referenceImportedToCase` | Keep one parent source and import exactly. | Mapper/write mapper, form/schema, backend API/model, Section C.3 import |
| `senderPostcode` | C.3.4.4 postcode | `SenderPresaveData.senderPostcode`; `SenderForm`; `senderPresaveSchema.senderPostcode` | `sender_presaves.postcode`; `SenderPresave.postcode`; `SenderPresaveForCreate/Update.postcode` | `senderPostcode`, `postcode` read only | `postcode` | `safetyReportIdentification.senderPostcode` | None | `referenceImportedToCase` | Keep one parent source and import exactly. | Mapper/write mapper, form/schema, backend API/model, Section C.3 import |
| `senderCountryCode` | C.3.4.5 country | `SenderPresaveData.senderCountryCode`; `SenderForm`; `senderPresaveSchema.senderCountryCode` | `sender_presaves.country_code`; `SenderPresave.country_code`; `SenderPresaveForCreate/Update.country_code` | `senderCountryCode`, `countryCode`, `country_code` read only | `country_code` | `safetyReportIdentification.senderCountryCode` | None | `referenceImportedToCase` | Keep one parent source and import exactly. | Mapper/write mapper, form/schema, backend API/model, Section C.3 import |
| `senderTelephone` | C.3.4.6 telephone | `SenderPresaveData.senderTelephone`; `SenderForm`; `senderPresaveSchema.senderTelephone` | `sender_presaves.telephone`; `SenderPresave.telephone`; `SenderPresaveForCreate/Update.telephone` | `senderTelephone`, `telephone` read only | `telephone` | `safetyReportIdentification.senderTelephone` | None | `referenceImportedToCase` | Keep one parent source and import exactly. | Mapper/write mapper, form/schema, backend API/model, Section C.3 import |
| `senderFax` | C.3.4.7 fax | `SenderPresaveData.senderFax`; `SenderForm`; `senderPresaveSchema.senderFax` | `sender_presaves.fax`; `SenderPresave.fax`; `SenderPresaveForCreate/Update.fax` | `senderFax`, `fax` read only | `fax` | `safetyReportIdentification.senderFax` | None | `referenceImportedToCase` | Keep one parent source and import exactly. | Mapper/write mapper, form/schema, backend API/model, Section C.3 import |
| `senderEmail` | C.3.4.8 email | `SenderPresaveData.senderEmail`; `SenderForm`; `senderPresaveSchema.senderEmail` | `sender_presaves.email`; `SenderPresave.email`; `SenderPresaveForCreate/Update.email` | `senderEmail`, `email` read only | `email` | `safetyReportIdentification.senderEmail` | None | `referenceImportedToCase` | Keep one parent source and import exactly. | Mapper/write mapper, form/schema, backend API/model, Section C.3 import |
| `regulatorGateways[].senderId` | FDA/MFDS Sender Id rows | `SenderPresaveData.regulatorGateways[].senderId`; `SenderForm` gateway rows | `sender_presave_gateways.sender_identifier`; `SenderPresaveGateway.sender_identifier` | `senderId`, `senderIdentifier`, `sender_identifier` read only | `gateways[].sender_identifier` | `messageHeader.messageSenderIdentifier` for matching authority | None | `referenceImportedToCase` | Keep gateway child row source; import exact FDA/MFDS authority row. | Mapper/write mapper, form/schema, backend API/model, submission/header import |
| `regulatorGateways[].as2RoutingId` | FDA/MFDS AS2 routing row value | `SenderPresaveData.regulatorGateways[].as2RoutingId`; `SenderForm` gateway rows | `sender_presave_gateways.routing_identifier`; `SenderPresaveGateway.routing_identifier` | `as2RoutingId`, `routingIdentifier`, `routing_identifier` read only | `gateways[].routing_identifier` | Empty | None | `referencePreserveOnly` | Preserve, do not import to C.3. | Mapper/write mapper, form/schema, backend API/model, no C.3 import |
| `regulatorGateways[].cdeSenderIdentifier` | NMPA CDE row value; local backend stores child field | `SenderPresaveData.regulatorGateways[].cdeSenderIdentifier`; `SenderForm` gateway rows | `sender_presave_gateways.cde_sender_identifier`; `SenderPresaveGateway.cde_sender_identifier` | `cdeSenderIdentifier`, `cde_sender_identifier` read only | `gateways[].cde_sender_identifier` | Empty | None | `referencePreserveOnly` | Preserve if present; no C.3 import. | Mapper/write mapper, form/schema, backend API/model, no C.3 import |
| `regulatorGateways[].cdrSenderIdentifier` | NMPA CDR row value; local backend stores child field | `SenderPresaveData.regulatorGateways[].cdrSenderIdentifier`; `SenderForm` gateway rows | `sender_presave_gateways.cdr_sender_identifier`; `SenderPresaveGateway.cdr_sender_identifier` | `cdrSenderIdentifier`, `cdr_sender_identifier` read only | `gateways[].cdr_sender_identifier` | Empty | Parent `cdrSenderId` removed; child gateway field retained | `referencePreserveOnly` | Preserve child row field only; no parent CDR source. | Mapper/write mapper rejects parent, type cleanup, backend API/model, no C.3 import |
| `regulatorGateways[].isDefaultForAuthority` | live default selector on gateway rows | `SenderPresaveData.regulatorGateways[].isDefaultForAuthority`; `SenderForm` gateway rows | `sender_presave_gateways.is_default_for_authority`; `SenderPresaveGateway.is_default_for_authority` | `isDefaultForAuthority`, `isDefault`, `is_default_for_authority` read only | `gateways[].is_default_for_authority` | Row selection only | Legacy `regulatorGatewayDefaultIndex` rejected by write tests | `referencePreserveOnly` | Persist row boolean; do not use index field. | Mapper/write mapper, form/schema, backend API/model |
| `senderPersons[].isDefault` | live default selector on responsible-person rows | `SenderPresaveData.senderPersons[].isDefault`; `SenderForm` responsible-person rows | `sender_presave_responsible_persons.is_default`; `SenderPresaveResponsiblePerson.is_default` | `isDefault`, `is_default` read only | `responsible_persons[].is_default` | Row selection only | Legacy `senderPersonDefaultIndex` rejected by write tests | `referencePreserveOnly` | Persist row boolean; do not use index field. | Mapper/write mapper, form/schema, backend API/model |
| `linkedOrganizationName` / `linkedOrganizationType` | no live reference sender field | None | None | None | None | Empty | Legacy local drift rejected by write tests | `removed` | Remove from sender presave path. | Write mapper negative test |
| parent `cdrSenderId` | CDR belongs to gateway child row | None | None | None | None | Empty | Legacy local drift removed from type/write path | `removed` | Remove parent field; keep only `regulatorGateways[].cdrSenderIdentifier`. | Type cleanup, write mapper negative test |
| `regulatorGatewayDefaultIndex` / `senderPersonDefaultIndex` | local UI-only indices | None | None | None | None | Empty | Legacy local drift rejected by write tests | `removed` | Replace with row booleans. | Write mapper negative test |

## Coverage Check

- FDA/MFDS live-relevant sender fields are classified.
- Local-only drift fields are classified as removed.
- PMDA/NMPA/EMA are explicitly out of scope.
- Total categorized rows: 25
- Uncategorized fields: 0
- Ambiguous fields: 0
