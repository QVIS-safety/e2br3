# Study Presave -> SI Reference Matrix

Date: 2026-06-01

## Scope

Reference routes:
- Study Presave list: `/:sponsorKey/:sessionSenderKey/:language/:appendix/caseinfo/study/list`
- Study Presave detail: `/:sponsorKey/:sessionSenderKey/:language/:appendix/caseinfo/study/:studyKey/detail`
- Case SI import workflow: case SI `C.5` library button -> Study select dialog -> `GET /1.1/sponsors/{sponsorKey}/studies/{studyKey}?CaseStudy`

Local modules compared:
- Frontend types/schema/mappers/forms: `lib/types/presave.ts`, `lib/schemas/presave.ts`, `lib/presave/canonicalMappers.ts`, `lib/presave/canonicalWriteMappers.ts`, `components/presave/StudyForm.tsx`
- Frontend SI import: `components/case-form/sections/SectionStudy.tsx`
- Backend model/schema/REST details: `crates/libs/lib-core/src/model/presave.rs`, `crates/services/web-server/src/web/rest/section_presave_rest.rs`, `db/bootstrap/01-safetydb-schema.sql`

## Reference Evidence

- UI list route is `CaseInfoStudyList`; visible list columns are `No.`, `Sponsor Study No.`, `Study Name`, `Study Type`, `EDC Sync`, `Product`, and `Deleted`.
- Bundle route evidence maps Study list/detail to `CaseInfo2` chunks and uses:
  - `GET /1.0/sponsors/{sponsor}/studies`
  - `GET /1.0/sponsors/{sponsor}/studies/{study}`
  - `PUT /1.0/sponsors/{sponsor}/studies/{study}`
  - `GET /1.1/sponsors/{sponsor}/studies/options`
  - `GET /1.1/sponsors/{sponsor}/studies/{study}?CaseStudy` for SI import.
- Reference Study Presave detail fields from `CaseInfo2.613bce31.js`:
  - Product selector: `selectedProducts[]`, submitted as `products[]` with `prdKey`, `prdName`.
  - Registration repeat table: `registrations[].stdRegNo`, `registrations[].stdRegCountry`, `registrations[].delFlag`.
  - `stdName` and `studyInfoNotation.stdName`.
  - `stdSpnNo` and KR-only `stdOid` selector.
  - `stdReactionType`.
  - `sync`, `excludeCaseKeyFromSync`, `delFlag`.
  - Reporter repeat table: `reporters[].rptKey`, `rptOrgan`, `rptGivName`, `rptQualify`, `delFlag`.
- Reference SI import from `CaseDetail1.b5394ea0.js`:
  - Opens Study select dialog, then calls `retrieveCaseSiStudyInfo(sponsorKey, STD_KEY)`.
  - Imports `registrations[].stdRegNo` -> `STD_REG_NO`.
  - Imports `registrations[].stdRegCountry` -> `STD_REG_COUNTRY`.
  - Imports `registrations[].delFlag` into the case registration row deleted state.
  - Imports `stdName` and `studyInfoNotation.stdName` into `C.5.2`.
  - Imports `stdSpnNo` into `C.5.3`.
  - Imports `stdReactionType` into `C.5.4`.
  - Imports `stdOid` into the KR-visible study number OID selector.
  - Does not import Study Presave product rows, EDC sync flags, deleted flag, reporters, FDA C.5.5/C.5.6 fields, or MFDS C.5.4.KR.1.

## Matrix

| field | reference evidence | canonical frontend source | canonical backend source | allowed read aliases | allowed write keys | case import target | duplicate sources found | category | decision | tests required |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Reference `STD_KEY` / local `id` | Study list/detail key and select dialog key. | `StudyPresaveData.id`. | `study_presaves.id`. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | Selector identity only. | None found. | `localSystemOnly` | Keep as local identity. | Identity, persistence, RLS/audit, or local display/helper coverage as applicable. |
| `organization_id` | Reference sponsor route scopes data. | Not exposed. | `study_presaves.organization_id` and RLS policies. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | Empty | None found. | `localSystemOnly` | Keep. | Identity, persistence, RLS/audit, or local display/helper coverage as applicable. |
| `name` | Local canonical template name, not visible as reference Study field. | Base presave name. | `study_presaves.name`. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | Empty | None found. | `localSystemOnly` | Keep as local template identity. | Identity, persistence, RLS/audit, or local display/helper coverage as applicable. |
| `comments` | No reference Study detail evidence. | Base comments/description path only. | `study_presaves.comments`. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | Empty | None found. | `localSystemOnly` | Keep only as local template metadata. | Identity, persistence, RLS/audit, or local display/helper coverage as applicable. |
| Reference `products[].prdKey` / `products[].prdName` | Detail product selector; required; submitted from `selectedProducts[]`; list column `Product`. | `studyProducts[]` maps `productPresaveId`, `productName`, `deleted`, row metadata; selected product writes a child row. | `study_presave_products` with REST/details graph and RLS/audit triggers. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | Not imported to SI. | None found. | `referencePreserveOnly` | Wired as preserve-only child rows. | Mapper/write mapper and backend API/model coverage; negative import coverage where applicable. |
| `product_presave_id` / `productPresaveId` | Local normalized FK for current required Study identity and product lookup. | Used by Study form and write mapper; child row also stores selected product FK. | `study_presaves.product_presave_id`; required for local identity. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | Empty | None found. | `localSystemOnly` | Keep as local identity/search helper while product rows carry reference preserve-only relationship. | Identity, persistence, RLS/audit, or local display/helper coverage as applicable. |
| `productId` | No reference field with this local canonical name; display-only value copied from Product Presave selector. | Present only as local form display state; no canonical details parent write. | No backend column. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | Empty | Display/helper value only; persisted product relationship uses `studyProducts[]` child rows. | `localSystemOnly` | Keep only as UI display helper, not a stored/imported Study Presave field. | Identity, persistence, RLS/audit, or local display/helper coverage as applicable. |
| `productName` | Reference has product display name inside `products[].prdName`, not parent `productName`. | Present only as selected product display state; child row persists `productName`. | No backend parent column. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | Empty | Display/helper value only; persisted product relationship uses `studyProducts[]` child rows. | `localSystemOnly` | Keep only as UI display helper. | Identity, persistence, RLS/audit, or local display/helper coverage as applicable. |
| `studyProductName` | No reference parent field; local display compatibility alias. | Present only as UI display state. | No backend column. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | Empty | Display/helper value only; persisted product relationship uses `studyProducts[]` child rows. | `localSystemOnly` | Keep only as UI display helper. | Identity, persistence, RLS/audit, or local display/helper coverage as applicable. |
| `studyProducts[].id` / `sequenceNumber` | Reference product row order. | Mapped and written. | `study_presave_products.id`, `sequence_number`. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | Empty | None found. | `localSystemOnly` | Keep as persistence mechanics. | Identity, persistence, RLS/audit, or local display/helper coverage as applicable. |
| `studyProducts[].productPresaveId` / `productName` | Reference product row key/name. | Mapped and written. | `study_presave_products.product_presave_id`, `product_name`. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | Empty | None found. | `referencePreserveOnly` | Wired. | Mapper/write mapper and backend API/model coverage; negative import coverage where applicable. |
| `studyProducts[].deleted` / `_delete` | Reference product row deleted state. | Mapped and written. | `study_presave_products.deleted`. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | Empty | None found. | `referencePreserveOnly` | Wired. | Mapper/write mapper and backend API/model coverage; negative import coverage where applicable. |
| Reference `registrations[].rowNo` / local `sequenceNumber` | Reference repeat row order. | `studyRegistrations[].sequenceNumber`; write mapper emits `sequence_number`. | `study_presave_registration_numbers.sequence_number`. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | Case repeat row order. | None found. | `localSystemOnly` | Keep sequence as persistence/import mechanics. | Identity, persistence, RLS/audit, or local display/helper coverage as applicable. |
| `registrations[].stdRegNo` / `studyRegistrations[].registrationNumber` | Reference imports into SI `STD_REG_NO`; max length 50. | Imported to `studyInformation.studyRegistrationNumbers[].registrationNumber`. | `study_presave_registration_numbers.registration_number` aligned to `VARCHAR(50)`. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | `C.5.1.r.1`. | None found. | `referenceImportedToCase` | Keep import with max length 50. | Mapper/write mapper, form/schema, backend API/model, and actual section import or negative import coverage. |
| `registrations[].stdRegCountry` / `studyRegistrations[].registrationCountry` | Reference imports into SI `STD_REG_COUNTRY`. | Imported to `studyInformation.studyRegistrationNumbers[].countryCode`. | `study_presave_registration_numbers.country_code`. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | `C.5.1.r.2`. | None found. | `referenceImportedToCase` | Keep exact country mapping. | Mapper/write mapper, form/schema, backend API/model, and actual section import or negative import coverage. |
| `registrations[].delFlag` / `studyRegistrations[].deleted` | Reference imports deleted state into case registration row state. | Section import keeps non-`_delete` rows and preserves `deleted` state. | Child table has `deleted`. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | Case registration repeat row deleted state. | None found. | `referenceImportedToCase` | Keep deleted-state import; ignore only transient `_delete` rows. | Mapper/write mapper, form/schema, backend API/model, and actual section import coverage. |
| `_delete` on study registration rows | Local transient client mutation flag. | Present in type/import filter. | Not persisted. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | Empty | None found. | `localSystemOnly` | Keep transiently if used by details update; do not expose as reference field. | Identity, persistence, RLS/audit, or local display/helper coverage as applicable. |
| `stdName` / `studyName` | Reference imports into SI `C.5.2`. | Required and imported to `studyInformation.studyName`. | `study_presaves.study_name`. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | `C.5.2`. | None found. | `referenceImportedToCase` | Keep. | Mapper/write mapper, form/schema, backend API/model, and actual section import or negative import coverage. |
| `studyInfoNotation.stdName` / `studyNameNotation` | Reference imports notation with `onLoad(stdName, notation)`. | Preserved in Study form/schema/write mapper and imported by `SectionStudy`. | `study_presaves.study_name_notation`. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | `C.5.2` notation. | None found. | `referenceImportedToCase` | Keep section import mapping for study name notation. | Mapper/write mapper, form/schema, backend API/model, and actual section import coverage. |
| `stdSpnNo` / `sponsorStudyNumber` | Reference imports into SI `STD_SPN_NO`; max length 50. | Required and imported. | `study_presaves.sponsor_study_number` aligned to `VARCHAR(50)`. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | `C.5.3`. | None found. | `referenceImportedToCase` | Keep import with max length 50. | Mapper/write mapper, form/schema, backend API/model, and actual section import or negative import coverage. |
| `stdOid` / local `sponsorStudyNumberKind` | Reference KR-visible OID selector imported into case study number OID; default `PROTOCOL_NO`; code list `STD_OID`. | `sponsorStudyNumberKind` is preserved and imported by `SectionStudy`. | `sponsor_study_number_kind`. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | KR `STD_OID` selector on `C.5.3`. | None found. | `referenceImportedToCase` | Keep one parent source and import to case study number kind. | Mapper/write mapper, form/schema, backend API/model, and actual section import coverage. |
| `stdReactionType` / `studyTypeReaction` | Reference imports into SI `STD_REACTION_TYPE`. | Required and imported to `studyInformation.studyTypeReaction`. | `study_presaves.study_type_reaction`. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | `C.5.4`. | None found. | `referenceImportedToCase` | Keep. | Mapper/write mapper, form/schema, backend API/model, and actual section import or negative import coverage. |
| `sync` / `edcSync` | Reference Study detail switch and list column; not imported by SI workflow. | Present as `edcSync`. | `study_presaves.edc_sync`. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | Empty | None found. | `referencePreserveOnly` | Keep as preserve-only. | Mapper/write mapper and backend API/model coverage; negative import coverage where applicable. |
| `excludeCaseKeyFromSync` | Reference Study detail switch; not imported by SI workflow. | `excludeCaseKeyFromSync` maps to parent payload. | `study_presaves.exclude_case_key_from_sync`. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | Empty | None found. | `referencePreserveOnly` | Wired as preserve-only. | Mapper/write mapper and backend API/model coverage; negative import coverage where applicable. |
| `delFlag` / `deleted` | Reference Study detail switch and list column; not imported to SI. | `deleted`. | `study_presaves.deleted`. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | Empty | None found. | `referencePreserveOnly` | Keep. | Mapper/write mapper and backend API/model coverage; negative import coverage where applicable. |
| `reporters[].rowNo` | Reference reporter repeat row order. | `studyReporters[].sequenceNumber` maps and writes. | `study_presave_reporters.sequence_number`. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | Empty | None found. | `referencePreserveOnly` | Wired. | Mapper/write mapper and backend API/model coverage; negative import coverage where applicable. |
| `reporters[].rptKey` | Reference reporter selector key. | `studyReporters[].reporterPresaveId` maps and writes. | `study_presave_reporters.reporter_presave_id`. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | Empty | None found. | `referencePreserveOnly` | Wired. | Mapper/write mapper and backend API/model coverage; negative import coverage where applicable. |
| `reporters[].rptOrgan` | Reference displays reporter organization. | `studyReporters[].reporterOrganization` maps and writes. | `study_presave_reporters.reporter_organization`. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | Empty | None found. | `referencePreserveOnly` | Wired. | Mapper/write mapper and backend API/model coverage; negative import coverage where applicable. |
| `reporters[].rptGivName` | Reference displays reporter given name. | `studyReporters[].reporterGivenName` maps and writes. | `study_presave_reporters.reporter_given_name`. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | Empty | None found. | `referencePreserveOnly` | Wired. | Mapper/write mapper and backend API/model coverage; negative import coverage where applicable. |
| `reporters[].rptQualify` | Reference displays reporter qualification. | `studyReporters[].reporterQualification` maps and writes. | `study_presave_reporters.reporter_qualification`. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | Empty | None found. | `referencePreserveOnly` | Wired. | Mapper/write mapper and backend API/model coverage; negative import coverage where applicable. |
| `reporters[].delFlag` | Reference reporter row deleted state. | `studyReporters[].deleted` / `_delete` maps and writes. | `study_presave_reporters.deleted`. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | Empty | None found. | `referencePreserveOnly` | Wired. | Mapper/write mapper and backend API/model coverage; negative import coverage where applicable. |
| `studyTypeReactionKr1` / `mfdsOtherStudiesType` | Reference SI has C.5.4.KR.1, but Study Presave detail and SI import do not set it from Study Presave. | Absent from Study Presave type/schema/form/write mapper; valid only as case SI field. | No active Study Presave backend source. | None. | None. | None from Study Presave. | Case-only authority field removed from Study Presave; remains only on case SI where valid. | `removed` | Keep removed from Study Presave and section import path. | Negative mapper/write/import/API tests prove the field is ignored or absent. |
| `mfdsStudyNumber` | No reference Study detail or SI import field. | Absent from active Study Presave type/schema/form/write mapper. | No active Study Presave backend source. | None. | None. | None. | Unsupported local field removed. | `removed` | Keep removed. | Negative mapper/write/API tests prove the field is ignored or absent. |
| `mfdsProtocolNumber` | No reference Study detail or SI import field. | Absent from active Study Presave type/schema/form/write mapper. | No active Study Presave backend source. | None. | None. | None. | Unsupported local field removed. | `removed` | Keep removed. | Negative mapper/write/API tests prove the field is ignored or absent. |
| `fdaIndNumberOccurred` | Reference SI has FDA.C.5.5a, but Study Presave detail and SI import do not set it from Study Presave. | Absent from Study Presave type/schema/form/write mapper; valid only as case SI field. | No active Study Presave backend source. | None. | None. | None from Study Presave. | Case-only authority field removed from Study Presave; remains only on case SI where valid. | `removed` | Keep removed from Study Presave and section import path. | Negative mapper/write/import/API tests prove the field is ignored or absent. |
| `fdaPreAndaNumberOccurred` | Reference SI has FDA.C.5.5b, but Study Presave detail and SI import do not set it from Study Presave. | Absent from Study Presave type/schema/form/write mapper; valid only as case SI field. | No active Study Presave backend source. | None. | None. | None from Study Presave. | Case-only authority field removed from Study Presave; remains only on case SI where valid. | `removed` | Keep removed from Study Presave and section import path. | Negative mapper/write/import/API tests prove the field is ignored or absent. |
| `fdaCrossReportedIndNumbers[].id` | Local child row ID only; reference Study Presave has no FDA cross IND child table. | Absent from active Study Presave type/schema/form/write mapper. | No active Study Presave child table. | None. | None. | None from Study Presave. | Unsupported FDA child source removed from Study Presave; remains only as case SI field group where valid. | `removed` | Keep removed with the FDA child table. | Negative mapper/write/import/API tests prove the field is ignored or absent. |
| `fdaCrossReportedIndNumbers[].sequenceNumber` | Local child row order only for unsupported child table. | Absent from active Study Presave type/schema/form/write mapper. | No active Study Presave child table. | None. | None. | None from Study Presave. | Unsupported FDA child source removed from Study Presave; remains only as case SI field group where valid. | `removed` | Keep removed with the FDA child table. | Negative mapper/write/import/API tests prove the field is ignored or absent. |
| `fdaCrossReportedIndNumbers[].indNumber` | Reference SI has FDA.C.5.6.r, but Study Presave detail and SI import do not set it from Study Presave. | Absent from Study Presave type/schema/form/write mapper; valid only as case SI field. | No active Study Presave child table. | None. | None. | None from Study Presave. | Case-only authority field removed from Study Presave; remains only on case SI where valid. | `removed` | Keep removed from Study Presave and section import path. | Negative mapper/write/import/API tests prove the field is ignored or absent. |
| `fdaCrossReportedIndNumbers[].deleted` / `_delete` | Local row state for unsupported child table. | Absent from active Study Presave type/schema/form/write mapper. | No active Study Presave child table. | None. | None. | None. | Unsupported FDA child source removed from Study Presave. | `removed` | Keep removed with the FDA child table. | Negative mapper/write/API tests prove the field is ignored or absent. |
| `created_at`, `updated_at`, `created_by`, `updated_by` | Reference has audit trail buttons, but these raw columns are local persistence metadata. | Not exposed directly. | Present on parent/child tables. | None unless explicitly named in canonical read mapper; read-only compatibility must not write back. | Canonical REST/write keys only; see canonical backend source. | Empty | None found. | `localSystemOnly` | Keep audit metadata for retained tables. | Identity, persistence, RLS/audit, or local display/helper coverage as applicable. |

## Category Summary

`referenceImportedToCase`:
`studyRegistrations[].registrationNumber`, `studyRegistrations[].registrationCountry`, `studyRegistrations[].deleted`, `studyName`, `studyNameNotation`, `sponsorStudyNumber`, `sponsorStudyNumberKind/stdOid`, `studyTypeReaction`

`referencePreserveOnly`:
`products[]`, `studyProducts[].productPresaveId`, `studyProducts[].productName`, `studyProducts[].deleted`, `edcSync`, `excludeCaseKeyFromSync`, `deleted`, `reporters[]`, `studyReporters[].reporterPresaveId`, `studyReporters[].reporterOrganization`, `studyReporters[].reporterGivenName`, `studyReporters[].reporterQualification`, `studyReporters[].deleted`

`localSystemOnly`:
`id`, `organization_id`, `name`, `comments`, `productPresaveId`, `productId` display helper, `productName` display helper, `studyProductName` display helper, product `id`/`sequenceNumber`, registration `sequenceNumber`, registration `_delete`, reporter `id`/`sequenceNumber`, audit metadata

`removed`:
`studyProducts[]` legacy aliases (`templateId`, `productId`, `medicinalProduct`, `drugBrandName`), `mfdsOtherStudiesType`, `mfdsStudyNumber`, `mfdsProtocolNumber`, `fdaIndNumberOccurred`, `fdaPreAndaNumberOccurred`, `fdaCrossReportedIndNumbers[]`

## Coverage Check

- Total reference fields/groups: 22
- Total local fields/groups: 43
- Total categorized rows: 43
- Uncategorized fields: 0
- Ambiguous fields: 0
